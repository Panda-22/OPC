智能体-投资建议
如何配置与启用

环境变量（建议在容器或服务器上配置）
USE_DEEPSEEK=true
DEEPSEEK_API_URL=https://api.deepseek.com/v1
DEEPSEEK_API_KEY=sk-3d061c2905d1401fac95925b1c8e6fb0
DEEPSEEK_MODEL=your_model_name (可选)
本地测试
在启动应用时设置环境变量，例如在 shell 中：
export USE_DEEPSEEK=true
export DEEPSEEK_API_URL=https://api.deepseek.com/v1
export DEEPSEEK_API_KEY=sk-3d061c2905d1401fac95925b1c8e6fb0
启动 uvicorn，然后通过 /upload_report 上传一个报告，接着用 /chat 提问。
如何测试基本流程

启动服务
uvicorn backend.main:app --reload --port 8000 --host 0.0.0.0
上传一个报告（文本/图片/PDF 均可）
通过前端 UI，或者 curl -F "file=@path/to/report.pdf" http://localhost:8000/upload_report
与智能体对话
curl -X POST http://localhost:8000/chat -H "Content-Type: application/json" -d '{"session_id":"","message":"核心结论是什么？"}'
观察结果
当 DeepSeek 可用且返回结果时，/chat 会返回 DeepSeek 的回答。
若DeepSeek不可用，/chat 将回退到规则引擎的回答。

export DEEPSEEK_API_URL="https://api.deepseek.com/v1/chat/completions"
export DEEPSEEK_API_KEY="sk-3d061c2905d1401fac95925b1c8e6fb0"
uvicorn backend.main:app --reload --port 8000 --host 0.0.0.0